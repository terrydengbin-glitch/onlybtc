# P2-C02 宏观雷达

## 状态

DONE

## 所属 Phase

P2

## 任务目标

围绕《开发文档.md》中对应 Phase 的设计，完成本任务所描述的能力建设，并保证产物可以被后续 Phase 复用。

## 背景依据

- [开发文档.md](../../开发文档.md)
- [task index.md](../../task%20index.md)

## 实施范围

- 明确本任务涉及的数据结构、接口、组件、任务或配置。
- 遵守 evidence + data、历史窗口、数据质量、反证机制和预警边界。
- 不输出交易建议，不引入开仓、止损、仓位或杠杆逻辑。

## 输入

- 上游 Phase 或前置任务产物。
- 开发文档中对应模块、雷达、总控、预警或 Dashboard 规范。

## 输出

- 可运行或可复用的代码、配置、Schema、接口、组件或文档。
- 必要的测试、验证记录或运行说明。

## 验收标准

- 与《开发文档.md》的总体架构一致。
- 任务产物能被后续任务引用。
- 关键状态、错误和数据质量可观测。
- 不绕过状态机、反方审查、预警等级或数据质量约束。

## 依赖任务

TBD

## 备注

已接入 `macro_radar` 雷达，当前读取：

- 基础宏观压力：`dxy_proxy`、`vix`、`ofr_fsi`
- 风险资产：`nasdaq`、`sp500`、`russell_2000`、`dow_jones`
- 避险与能源：`gold`、`wti_oil`、`brent_oil`

所有指标均通过 P1/P8 标准链路进入：`collect_sources -> SQLite metric_values -> historical_window -> radar feature`。P2 不直接访问 provider，不自行选择主源；主源、fallback、冲突、freshness 均由 P1/P8 输出。

P1-C22 中文 HTML 审计中，以上指标必须显示为“已写入”，且 `macro_radar` 消费项应显示为“是”。
