# P4.5-C29 / Crypto Breadth v3 报告层趋势确认与宽度背离解释

## 状态

DONE

## 目标

报告层解释 `crypto_breadth.v3` 的 BTC 趋势确认、风险扩散、宽度背离与 alt rotation，不再写成简单 crypto breadth bullish/bearish。

## 文案约束

禁止：

```text
BTC dominance 上升，所以 bullish
ETH/BTC 上升，所以 BTC bullish
sector_heat 高，所以继续看多
```

允许：

```text
BTC 上涨获得 Top50 与 TOTAL2 扩散确认，趋势质量较高。
BTC 上涨但 Top50 参与度不足，属于窄幅 BTC rally，追多置信度下降。
ETH/BTC 与 TOTAL2 修复说明 crypto risk-on 扩散，但不等于 BTC 一定跑赢。
sector_heat 过高时提示追 beta 风险，而不是继续加方向分。
```

## DoD

- P4.5 final payload 透出 `crypto_breadth_explanation`。
- 报告层能解释 `crypto_breadth_state` 和 `btc_implication`。
- `support_drivers / pressure_drivers / risk_drivers` 按 v3 语义输出。
- 不出现单指标误导文案。

## Tests

```powershell
.\.venv\Scripts\python.exe -m pytest backend\tests\test_p45_final_writer.py backend\tests\test_p45_evidence_pack.py -q
```

## Verification

```powershell
.\.venv\Scripts\python.exe -m pytest backend\tests\test_p45_dashboard_api.py -q
```
