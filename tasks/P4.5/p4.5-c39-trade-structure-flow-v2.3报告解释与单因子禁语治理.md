# P4.5-C39 / Trade Structure Flow v2.3 报告解释与单因子禁语治理

## 状态

DONE

## Phase

P4.5 Radar Scored Analyst Writer

## 背景

报告层必须解释 `trade_structure_flow v2.3` 的交易结构确认/反证语义，并明确禁止把成交量、taker ratio、funding、OI、清算 spike 单独解释成 BTC 方向。

## 目标

新增报告解释：

```text
trade_structure_flow 不预测单因子方向
structure pressure 只是压力
BTC price acceptance 才确认方向
standardized residual 用于判断接受/拒绝
liquidation snapshot 不能单独 confirmed
```

## 禁语

```text
成交量高所以 BTC 看多
taker buy 高所以 BTC 看多
taker sell 高所以 BTC 看空
funding positive 所以 bullish
OI rising 所以 bullish/bearish
liquidation spike 所以 confirmed
liquidity thinning 所以 bearish
```

## DoD

- [ ] final payload 包含 `trade_structure_flow_explanation` v2.3 字段。
- [ ] 报告解释包含 signal_stage、trade_structure_state、btc_implication。
- [ ] 报告说明 residual 是标准化 residual，不混用 score 与 return。
- [ ] 禁语不会出现在 deterministic summary 或 LLM prompt guidance 中。
- [ ] 测试覆盖报告层解释与禁语边界。

## 关联任务

- P3-C53
- P9-C35
- P5-C56
