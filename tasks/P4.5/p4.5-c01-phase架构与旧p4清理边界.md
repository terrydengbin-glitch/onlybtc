# P4.5-C01 Phase 架构与旧 P4 清理边界

## 状态

DONE

## 所属 Phase

P4.5 Radar Scored Analyst Writer

## 任务目标

建立 P4.5 的工程边界、目录结构、CLI/API 命名和 legacy P4 隔离策略，明确哪些旧 P4 组件保留、迁移、删除或停用。

## 实施范围

- 新增 P4.5 phase 文档和模块目录。
- 明确 P4.5 不再使用 Agent/CrossExam/Judge/Adversarial Review。
- 保留可复用的 LLM provider、HTML 工具、SQLite snapshot/history 能力。
- 定义旧 P4 legacy 标记和兼容窗口。
- 定义 P5/P6 后续改为消费 P4.5 输出。

## 验收标准

- 任务索引存在 P4.5 独立 Phase。
- 旧 P4 与新 P4.5 的职责边界清晰。
- 后续任务可以按 P4.5-C02 起顺序实施。

## 完成记录

已完成 P4.5 工程边界与旧 P4 legacy 隔离基线：

- 新增 P4.5 Python package：
  - `backend/src/onlybtc/p45/__init__.py`
  - `backend/src/onlybtc/p45/boundary.py`
  - `backend/src/onlybtc/p45/README.md`
- `phase_boundary()` 明确：
  - P0/P1/P2/P3/P8 上游契约。
  - 4 个 Analyst 与 14 个 Radar module 的覆盖关系。
  - P4.5 输出产物。
  - 旧 P4 Agent/CrossExam/Judge/Adversarial Review/Final Controller 不进入 P4.5 默认链路。
- P4.5 phase 方案状态从 `PROPOSAL` 调整为 `ACTIVE`。
- 旧 P4 代码暂不删除，仅继续作为 legacy/manual/debug 链路保留。

验证：

```powershell
.\.venv\Scripts\python.exe -m pytest backend/tests/test_p45_boundary.py -q
.\.venv\Scripts\python.exe -m ruff check backend/src/onlybtc/p45 backend/tests/test_p45_boundary.py
```

结果：

- `2 passed`
- `ruff passed`
