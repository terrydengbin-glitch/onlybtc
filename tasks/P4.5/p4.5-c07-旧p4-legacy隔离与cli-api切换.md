# P4.5-C07 旧 P4 Legacy 隔离与 CLI/API 切换

## 状态

DONE

## 所属 Phase

P4.5 Radar Scored Analyst Writer

## 任务目标

在 P4.5 跑通后，将旧 P4 Agent 链路隔离为 legacy，并将默认 CLI/API 切换到 P4.5。

## 实施范围

- 新增 `p4.5-full-audit` 或等价 CLI。
- 将默认文章链路切换到 P4.5。
- 旧 P4 CLI 保留 legacy/manual/debug 用途。
- 移除 P5 对旧 debate/judge/revision 的默认依赖。
- 更新开发文档和任务索引。

## 验收标准

- 默认全链条使用 P4.5。
- 旧 P4 不影响生产链条。
- 如需回看旧 P4，必须显式 legacy 参数。

## 完成记录

2026-05-22 已完成 P4.5 CLI/API 入口切换基线：

- 新增 `backend/src/onlybtc/audit/p45_full_chain.py`。
- 新增 CLI：
  - `p45-full-audit`
  - `p45-build-evidence-pack`
  - `p45-run-analysts`
  - `p45-final-writer`
  - `p45-html-report`
- 新增 API：
  - `POST /api/p45/evidence-pack`
  - `POST /api/p45/analyst-articles`
  - `POST /api/p45/final-article`
  - `POST /api/p45/html-report`
- 旧 P4 命令继续保留为 legacy/manual/debug 用途，不再作为新生产主线。

验证：

- `python -m onlybtc.cli --help` 可见 P4.5 命令。
- `pytest tests/test_p45_*.py tests/test_api.py -q` 通过，12 passed。
- `ruff check src/onlybtc/p45 src/onlybtc/audit/p45_full_chain.py src/onlybtc/cli.py src/onlybtc/api/app.py tests/test_p45_*.py tests/test_api.py` 通过。
