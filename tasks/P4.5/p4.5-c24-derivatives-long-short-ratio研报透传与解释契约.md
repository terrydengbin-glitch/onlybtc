# P4.5-C24 / Derivatives Long/Short Ratio 研报透传与解释契约

## 状态

DONE

## 背景

P3-C34 会为 `derivatives_crowding` 增加 long/short ratio 组合语义。P4.5 需要把这些字段进入 Evidence Pack、Final JSON、HTML 报告和中文解释，避免文章继续只写 Funding/OI。

## 目标

1. Evidence Pack 透传 long/short ratio 指标级语义字段。
2. Final JSON 透传 derivatives module 复合状态。
3. P4.5 研报能解释“多空偏斜、拥挤风险、挤压风险”。
4. 发文层不把 long ratio 高写成强看多。
5. Radar Detail API 可以消费同一套字段。

## 透传字段

模块级：

```text
positioning_state
top_positioning_state
positioning_conflict_level
long_short_squeeze_risk
long_short_combo_applied
```

指标级：

```text
positioning_signal
crowding_contribution
trend_confirmation
```

## 文章语义

禁止：

```text
多头比例高，所以衍生品看多。
```

推荐：

```text
多头比例偏高时，需要结合 Funding 和 OI 判断是否拥挤。若多头比例高、Funding 升温且 OI 扩张，更像多头拥挤风险，而不是健康看多确认。
```

## DoD

- [ ] P4.5 Evidence Pack 保留新增字段。
- [ ] Final JSON 的 `radar_module_scores` 保留 derivatives long/short 复合语义。
- [ ] HTML 报告显示 derivatives positioning / squeeze risk。
- [ ] Publish article 不出现“long ratio 高 = 直接 bullish”的错误表达。
- [ ] P4.5 contract validation 增加 long/short 字段透传检查。
- [ ] P4.5 测试通过。

## 关联

P3-C34, P4.5-C23, P9-C20, P5-C41

## Completion Note

- Done: Evidence Pack, Final JSON and HTML flattening preserve derivatives positioning fields.
- Verified: P4.5 final writer and dashboard API tests passed.
