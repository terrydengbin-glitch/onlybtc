# P7-C01 模块权重动态调整

## 状态

DONE

## Execution Record

### 2026-06-23 / Start

- 用户要求继续，按建议启动 P7-C01。
- 当前生产权重来自 `onlybtc.radars.registry.MODULE_WEIGHTS`，P4.5 `final_writer` 使用模块 payload 中的 `module_weight` 进行融合。
- 本任务先实现 **动态权重建议层**：生成可审计、可回滚、可复用的 profile / recommendation，不直接改生产 registry 权重，不改变状态机、预警、BTC score 或交易语义。

### 2026-06-23 / DONE

- 新增 `backend/src/onlybtc/calibration/module_weights.py`：base 权重归一化、scenario profile、data quality discount、profile recommendation、rollback guardrails。
- 新增 `scripts/generate_p7_c01_module_weight_calibration.py`：生成 JSON/MD 审计报告。
- 新增 `backend/tests/test_p7_module_weights.py`：覆盖 registry 不变、权重归一化/cap、event_window profile 提升、低质量模块降权、recommendation_only rollback。
- 生成报告：
  - `reports/p7-c01-module-weight-calibration-report.json`
  - `reports/p7-c01-module-weight-calibration-report.md`
- 当前 latest module payload 未触发动态 profile 阈值，安全推荐 `selected_profile=base`，`applied_to_production=false`。
- 验证：
  - `.\.venv\Scripts\python.exe -m pytest backend\tests\test_p7_module_weights.py -q` 通过：4 passed。
  - `.\.venv\Scripts\python.exe scripts\generate_p7_c01_module_weight_calibration.py` 通过。
  - `compileall` 通过。

## 所属 Phase

P7

## 任务目标

围绕《开发文档.md》7.1 / 7.6 / 17.2，建立模块权重动态调整的可审计建议层。

本卡不直接替换生产权重，而是输出：

```text
base registry weights
scenario profiles
data-quality adjusted recommendation
rollback base
audit report
```

供后续 P7-C02、P7-C08 和 P9 API/UI 消费。

## 背景依据

- [开发文档.md](../../开发文档.md)
- [task index.md](../../task%20index.md)

## 实施范围

- 新增纯函数模块：动态权重 profile / normalization / cap / data quality discount。
- 新增报告脚本：生成 `reports/p7-c01-module-weight-calibration-report.json/md`。
- 新增测试：验证权重总和、cap、profile 提升、data quality 降权、生产权重不被修改。
- 遵守 evidence + data、历史窗口、数据质量、反证机制和预警边界。
- 不输出交易建议，不引入开仓、止损、仓位或杠杆逻辑。

## 输入

- `onlybtc.radars.registry.MODULE_WEIGHTS`
- P4.5 latest dashboard / `radar_module_scores`（可选；缺失时仍生成 base/profile 报告）
- `reports/btc-4h-1d-direct-trend-evaluation-report.json`（可选审计引用）
- 开发文档 Phase 7 权重动态调整规范。

## 输出

- `backend/src/onlybtc/calibration/module_weights.py`
- `scripts/generate_p7_c01_module_weight_calibration.py`
- `backend/tests/test_p7_module_weights.py`
- `reports/p7-c01-module-weight-calibration-report.json`
- `reports/p7-c01-module-weight-calibration-report.md`

## 验收标准

- [x] 与《开发文档.md》的总体架构一致。
- [x] 输出 base/profile/recommendation/rollback，且 `applied_to_production=false`。
- [x] profile 权重归一化，总和为 1.0，单模块 cap 可审计。
- [x] 数据质量差的模块会被降权，但不会被删除。
- [x] Event window profile 提升 event/options/macro 相关模块，同时标记 confidence discount。
- [x] 任务产物能被 P7-C02/P7-C08/P9 引用。
- [x] 不绕过状态机、反方审查、预警等级或数据质量约束。
- [x] focused tests 通过。
- [x] 报告生成通过。

## 依赖任务

- P7-C09/P7-C10/P7-C13/P7-C31 评估与审计产物。
- P6 replay score 完成后可替换当前 profile heuristic。

## 备注

- 本任务不直接改 `MODULE_WEIGHTS`，只生成建议层与审计报告。
- 后续若要上线动态权重，必须经 P7-C08 mock DoD 和 P9 API 契约暴露。
