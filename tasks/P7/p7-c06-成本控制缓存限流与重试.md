# P7-C06 成本控制、缓存、限流与重试

## 状态

DONE

## 所属 Phase

P7

## 任务目标

建立成本控制、缓存、限流与重试的治理审计层，汇总 source collection、FRED throttle、Playwright concurrency、LLM runtime budget、P4.5 research retry/timeout、SQLite RateLimit/Fallback 事件和本地 cache 目录状态，输出可供 P7-C08 生产化 mock 验收引用的报告。

## 背景依据

- [开发文档.md](../../开发文档.md)
- [task index.md](../../task%20index.md)

## 实施范围

- 新增 governance module 汇总成本/限流/重试/缓存配置和运行事件。
- 读取 `Settings` 中 source concurrency/retry/backoff/FRED throttle/LLM max calls/token budget/timeout/retry/fallback policy。
- 读取 SQLite `RateLimitEvent`、`FallbackEvent`，形成近期限流与 fallback 成本风险摘要。
- 检查本地 `cache/` 目录是否存在、文件数量和大小。
- 输出 `reports/p7-c06-cost-control-cache-rate-limit-report.json/md`。
- 本任务不直接调整采集、LLM、缓存、限流或重试生产参数。
- 遵守 evidence + data、历史窗口、数据质量、反证机制和预警边界。
- 不输出交易建议，不引入开仓、止损、仓位或杠杆逻辑。

## 输入

- `onlybtc.core.config.Settings`
- `rate_limit_events`
- `fallback_events`
- `cache/`
- P4 AgentRuntime budget guard
- P4.5 LLM writer timeout/retry config
- P7-C04/P7-C05 source health and Playwright stability outputs

## 输出

- Governance module：`backend/src/onlybtc/governance/cost_controls.py`。
- 报告生成脚本：`scripts/generate_p7_c06_cost_control_report.py`。
- 测试：`backend/tests/test_p7_cost_controls.py`。
- 报告：`reports/p7-c06-cost-control-cache-rate-limit-report.json/md`。

## 验收标准

- [x] 报告明确 `applied_to_production=false`。
- [x] source concurrency、retry、backoff、FRED throttle 配置可观测。
- [x] P4 LLM max calls / estimated token budget / fallback policy 可观测。
- [x] P4.5 timeout/retry 与缺少显式 token/call budget 的风险可观测。
- [x] RateLimitEvent / FallbackEvent 能触发告警项。
- [x] cache 目录状态可观测，报告不包含 API key 或 token。
- [x] 测试覆盖健康配置、限流事件、缓存统计、报告生成。
- [x] 不绕过状态机、反方审查、预警等级或数据质量约束。

## 执行记录

- 新增 `backend/src/onlybtc/governance/cost_controls.py`。
- 新增 `scripts/generate_p7_c06_cost_control_report.py`。
- 新增 `backend/tests/test_p7_cost_controls.py`。
- 生成 `reports/p7-c06-cost-control-cache-rate-limit-report.json`。
- 生成 `reports/p7-c06-cost-control-cache-rate-limit-report.md`。
- 当前报告 `overall_status=warning`，主要告警：
  - `p45_research_budget_gap`：P4.5 research writer 有 timeout/retry，但缺少显式 call/token budget。
  - `fallback_events_present`：当前 SQLite 中 fallback_event_count=105。
- 当前 source collection 配置已有 retry/backoff，FRED 有 batch delay/jitter/backoff，P4 LLM 有 max calls 和 estimated token budget。
- 当前 cache 目录存在，file_count=398，total_bytes=66591981。

## 验证

- `PYTHONPATH=backend/src .\.venv\Scripts\python.exe -m pytest backend\tests\test_p7_cost_controls.py -q`：5 passed。
- `PYTHONPATH=backend/src .\.venv\Scripts\python.exe scripts\generate_p7_c06_cost_control_report.py`：通过，生成 JSON/MD 报告。
- `PYTHONPATH=backend/src .\.venv\Scripts\python.exe -m compileall backend\src\onlybtc\governance scripts\generate_p7_c06_cost_control_report.py`：通过。

## 依赖任务

- P7-C04
- P7-C05
- P7-C08

## 备注

- 本任务先做审计和治理可观测性。生产参数调整由 P7-C08 mock 验收后再进入配置或运行链。
- 本任务未修改采集、LLM、缓存、限流或重试生产参数。
