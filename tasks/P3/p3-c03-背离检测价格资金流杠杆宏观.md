# P3-C03 背离检测：价格、资金流、杠杆、宏观

## 状态

DONE

## 实现结果

已在 `backend/src/onlybtc/algorithms/p3.py` 实现 `detect_divergences()`。

输出写入：

```yaml
table: feature_values
module_id: p3_divergence_engine
feature_id: "{divergence_type}.divergence"
metadata:
  divergence_id
  type
  direction
  severity_candidate
  price_metric
  price_change
  metrics
  data_quality
```

## 覆盖背离类型

- `price_vs_fund_flow`
- `price_vs_leverage`
- `price_vs_macro`
- `price_vs_onchain`

## 对齐规则

- 使用 P1 selected source，不重新选源。
- 使用 P2/P1 的质量、freshness 与 conflict 作为降级依据。
- `whale_flow/miner_flow` 仍按 provider-required 限制处理，不作为强证据。

## 验收

- Mock 场景可产生资金流/杠杆背离。
- 输出可被 P3-C06 消费。
- 后端全量测试通过：51 passed。

