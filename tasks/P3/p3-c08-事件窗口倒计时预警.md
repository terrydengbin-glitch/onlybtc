# P3-C08 事件窗口 T-7/T-3/T-1/T-0/T+1/T+3

## 状态

DONE

## 实现结果

已在 `backend/src/onlybtc/algorithms/p3.py` 实现 `detect_event_windows()`。

输入指标：

- `cpi_days_until`
- `fomc_days_until`
- `pce_days_until`
- `nfp_days_until`

输出写入：

```yaml
table: feature_values
module_id: p3_event_window_engine
feature_id: "{metric_id}.event_window"
metadata:
  event_id
  event_type
  days_until
  window
  alert_level
  risk_lock
  source_id
  quality_score
  action
```

## 窗口规则

- `T-7`：info
- `T-3`：watch
- `T-1`：warning + risk_lock
- `T-0`：warning + risk_lock
- `T+1`：watch + risk_lock
- `T+3`：info

## 验收

- CPI/FOMC/PCE/NFP 可生成事件窗口状态。
- 事件窗口可进入 P3-C06 预警。
- 输出可供 P5 Dashboard / Alerts / History Replay 使用。

