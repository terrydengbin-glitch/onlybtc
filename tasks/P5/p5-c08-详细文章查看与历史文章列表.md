# P5-C08 详细文章查看与历史文章列表

## 状态

DONE

## 当前架构对齐（2026-05-22）

文章页主数据改为 P4.5 Report v2：`research_article`、`publish_article`、`decision_card`、`aggregation_audit`、`horizon_views`、`contract_validation`、LLM research article internal_reference。

FastAPI 读取：`GET /api/p45/articles/latest`、`GET /api/p45/history/{final_run_id}`。

文章页必须明确区分：研究报告正文、发文版本、LLM 深度研报 internal_reference、deterministic analyst appendix。旧 P4-C17 文章不是当前主线，只能作为 legacy 历史文章。

## 所属 Phase

P5

## 任务目标

展示 P4-C17 生成的中文观察文章、4 个分析师文章段落、最终观察建议、证据引用和历史文章列表。

## 背景依据

- [开发文档.md](../../开发文档.md)
- [task index.md](../../task%20index.md)

## 实施范围

- 文章必须展示 `final_run_id`、`pack_id`、`article_run_id`、`llm_research_run_id`、`article_runtime_mode`、`article_status`。
- 展示 evidence citations，可跳转 Evidence。
- 标注文章是否 stale、draft、blocked、published 或 fallback generated。
- 历史文章列表可按 snapshot / run / status 过滤。
- 遵守 evidence + data、历史窗口、数据质量、反证机制和预警边界。
- 不输出交易建议，不引入开仓、止损、仓位或杠杆逻辑。

## 输入

- P9-C03 Article API
- P4-C17 中文文章输出
- P4-C18 runtime governance 字段

## 输出

- 可运行或可复用的代码、配置、Schema、接口、组件或文档。
- 必要的测试、验证记录或运行说明。
- FastAPI 新增 `GET /api/p45/articles/history`，用于文章历史快照列表。
- Vue3 Article Center 已展示 run lineage、runtime/status、发文版、研究正文、LLM internal_reference、四分析师文章、Evidence citations 与 History Snapshots。
- Evidence citation chip 可跳转 Evidence detail；历史快照可调用 `/api/p45/history/{final_run_id}` 进入 replay。

## 验收标准

- 与《开发文档.md》的总体架构一致。
- 文章中关键结论必须可追溯到 evidence + data + history。
- 关键状态、错误和数据质量可观测。
- 不绕过 P4.5 final_view、反证/确认规则、预警等级或数据质量约束。

## 验收记录（2026-05-22）

```powershell
npm run build
.\.venv\Scripts\python.exe scripts\validate_p5_dashboard_contract.py
.\.venv\Scripts\python.exe scripts\validate_p5_page_dod.py
Invoke-RestMethod "http://127.0.0.1:8118/api/p45/articles/history?limit=3"
```

结果：

- 前端 build passed。
- P5 Dashboard contract validation passed。
- P5 page DoD validation passed。
- Article history API 返回 `status=ok`，并包含多个 `final_run_id` 快照。

## 依赖任务

P4-C17、P4-C18、P5-C10、P5-C25、P9-C03

## 备注

文章是观察建议和证据解释，不是交易计划。
