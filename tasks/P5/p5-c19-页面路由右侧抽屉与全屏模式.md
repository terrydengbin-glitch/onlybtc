# P5-C19 页面路由、右侧抽屉与全屏模式

## 状态

DONE

## 当前架构对齐（2026-05-22）

路由上下文必须携带 P4.5 run lineage 和点击对象：`final_run_id`、`pack_id`、`module_id`、`evidence_id`、`source_id`、`analyst_id`。

右侧抽屉默认 Tab：Overview、Radar、Evidence、Article、Analysts / LLM Appendix、Alerts、Invalidation、Data Quality、Run Logs、History。旧 P4 Debate 只能作为 legacy tab。

## 所属 Phase

P5 Dashboard 全量可视化

## 任务目标

实现 Vue3 页面路由、右侧详情抽屉、全屏子页面、Quick View 和跨页面跳转。

## UI 依据

- [ui方案-p5-dashboard.md](../../ui方案-p5-dashboard.md)

## FastAPI 依赖

- P9-C01 API DTO 与错误响应。

## SQLite 依赖

- 通过各页面 API 间接依赖。

## 实施范围

- 页面路由。
- 右侧抽屉 tabs：Overview / Article / Evidence / LLM Appendix / Alerts / Invalidation / Data Quality / Run Logs。
- 全屏模式：LLM Appendix / Evidence / Data Quality / Run Logs / History Replay。
- 关闭按钮规范。
- 面包屑和返回规则。
- 所有路由和抽屉上下文都必须携带 `final_run_id`、`pack_id` 与当前 run context。
- 历史模式路由必须带 `final_run_id`，并禁止实时订阅覆盖页面数据。

## 验收标准

- 每个入口映射到正确子页面。
- 右上角关闭按钮不被设置齿轮替代。
- 历史模式和实时模式路由分离。
- 从任意 evidence/source/radar/agent chip 跳转后，返回路径必须回到原 Dashboard 上下文。

## 完成记录

- 已补齐前端 `routeContext`：`final_run_id`、`pack_id`、`module_id`、`evidence_id`、`source_id`、`analyst_id`、`isHistorical`。
- 已实现 URL query 与页面状态同步：`page`、`final_run_id`、`pack_id`、`module_id`、`evidence_id`、`source_id`、`analyst_id`、`mode=history`、`fullscreen=1`。
- 已支持从 URL 恢复 radar/evidence/source/history 选中上下文。
- 已增加右侧 Context Drawer：展示当前页面、运行模式、run lineage 和点击对象上下文。
- 已增加关闭抽屉、重新打开抽屉、Back Dashboard、Fullscreen / Exit Fullscreen 规范按钮。
- 已保留 Summary / Decision / LLM / Quick Links，并补充 History、Settings 入口。
- 验证通过：
  - `npm run build`
  - `python scripts/validate_p5_dashboard_contract.py`
  - `python scripts/validate_p5_page_dod.py`
